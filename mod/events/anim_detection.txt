namespace = anim_detection_events

# detect province stuff
province_event = {
    id = anim_detection_events.1
    hidden = yes

    trigger = {
        has_siege = yes
    }

    mean_time_to_happen = {
        days = 1
    }

    option = {
        ai_chance = 1
    }

    every_country = {
        limit = {
            Root = {
                sieged_by = Prev
            }
        }

        log = "ANIMATED_REPLAY on_province_siege_progress||[Root.GetCapitalName]||[This.GetTag]"
    }
}